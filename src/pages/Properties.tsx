
import Associations from './Associations';
import { PropertyUI } from '@/types/property-types';

// For backward compatibility with existing imports
export type Property = PropertyUI;

export default Associations;
