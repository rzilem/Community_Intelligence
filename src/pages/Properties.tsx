
import React from 'react';
import PageTemplate from './PageTemplate';
import { Building } from 'lucide-react';

const Properties = () => {
  return <PageTemplate title="Properties" icon={<Building className="h-8 w-8" />} />;
};

export default Properties;
