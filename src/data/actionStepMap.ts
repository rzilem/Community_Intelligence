export type ActionStepMap = { [actionStep: string]: string };

// Mapping of action steps to workflow template IDs.
// TODO: Replace placeholder values with IDs from the spreadsheet.
export const actionStepMap: ActionStepMap = {
  // Example entries:
  // 'New Resident Onboarded': 'template-id-123',
  // 'Payment Received': 'template-id-456'
};
