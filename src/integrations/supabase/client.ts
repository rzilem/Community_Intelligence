
// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import type { Database } from './types';

const SUPABASE_URL = "https://eqbbnewrorxilukaocjx.supabase.co";
const SUPABASE_PUBLISHABLE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVxYmJuZXdyb3J4aWx1a2FvY2p4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDMwMDk1OTksImV4cCI6MjA1ODU4NTU5OX0.VR0HEuV67Sp6js9tujvAqut0uf6342baidyAvQLwKaQ";

// Import the supabase client like this:
// import { supabase } from "@/integrations/supabase/client";

export const supabase = createClient<Database>(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, {
  auth: {
    persistSession: true,
    autoRefreshToken: true,
    storage: localStorage
  }
});

// Test connection and log info
(async () => {
  try {
    console.log('Testing Supabase connection...');
    const { data: version } = await supabase.from('_supabase_version').select('*');
    console.log('Supabase connection successful:', version);
    
    // Log available tables
    const { data: tables, error: tablesError } = await supabase.from('supabase_schema_information').select('*');
    if (tablesError) {
      console.error('Error fetching schema information:', tablesError);
    } else {
      console.log('Available tables:', tables);
    }
  } catch (error) {
    console.error('Error testing Supabase connection:', error);
  }
})();
